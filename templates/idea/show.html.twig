{% extends 'base.html.twig' %}

{% block title %}| Idea
{% endblock %}

{% block body %}
	<main class="ideaz d-flex flex-column align-items-center mt-5">
		<h1 class="text-white my-4">{{ project.name }}</h1>

		<section class="testBubble row align-items-center mt-5 justify-content-center">
			{% for idea in ideas %}
			<div class="box d-flex justify-content-center align-items-center">
				<h3 class="text-white text-center">{{ idea.name }}</h3>
			</div>
			{% endfor %}
			<div class="box d-flex justify-content-center align-items-center">
				<h3 class="text-white text-center">Another idea</h3>
			</div>
			<div class="box d-flex justify-content-center align-items-center">
				<h3 class="text-white text-center">Complement idea</h3>
			</div>
			<div class="box d-flex justify-content-center align-items-center">
				<h3 class="text-white text-center">Something special for the project</h3>
			</div>
		</section>



	</main>
{% endblock %}

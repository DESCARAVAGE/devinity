<form method="post" action="{{ path('app_idea_delete', {'id': idea.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ idea.id) }}">
    <button class="btn">Delete</button>
</form>
